// TODO: Export Environment to the root of flocking_cpal.

#[derive(Debug)]
pub struct Environment {}

impl Environment {
    pub fn new(settings: flocking::EnvironmentSettings) -> Environment {
        Environment {}
    }
}
